---
title: "Mann-Whitney U 검정 결과를 APA 형식으로 보고하는 방법 (예시 포함)"
description: "Mann-Whitney U 검정 결과를 APA 7판 형식으로 보고하는 완전 가이드. U 통계량, z-점수, 정확한 p-값, 효과크기 r, 복사-붙여넣기 템플릿 포함."
date: "2026-03-02"
category: "APA 보고법"
tags: ["Mann-Whitney U", "APA 형식", "비모수 검정", "효과크기", "통계 보고", "윌콕슨 순위합 검정"]
---

## Mann-Whitney U 검정이 필요한 경우

Mann-Whitney U 검정(윌콕슨 순위합 검정이라고도 함)은 독립표본 t-검정의 비모수적 대안입니다. 두 독립 집단을 서열 또는 연속 변수에서 비교할 때 사용하며, 다음 조건 중 하나 이상에 해당할 때 적합합니다:

- 종속변수가 한쪽 또는 양쪽 집단에서 정규분포를 따르지 않는 경우
- 표본 크기가 작은 경우 (일반적으로 집단당 *n* < 30)
- 데이터에 심각한 이상치가 있는 경우
- 변수가 서열 척도로 측정된 경우

## APA 보고의 필수 구성요소

APA 7판에서 Mann-Whitney U 결과를 보고할 때 포함해야 하는 요소:

- **U 통계량**: Mann-Whitney *U* 값
- **표본 크기**: 각 집단의 *n*
- **z-점수**: 표준화된 검정통계량
- **정확한 *p* 값**: 소수점 셋째 자리까지
- **효과크기**: 순위이연상관(*r*) 또는 *r* = *z* / &radic;*N*
- **중앙값과 사분위범위**: 각 집단별

## 1단계: 기술통계 보고

비모수 검정에서는 평균과 표준편차 대신 **중앙값**(*Mdn*)과 사분위범위(IQR)를 보고합니다.

**예시 시나리오:** 치료군(*n* = 25)과 대조군(*n* = 25)의 통증 평가(0-10 척도)를 비교합니다.

| 집단 | *n* | *Mdn* | IQR |
|------|-----|-------|-----|
| 치료군 | 25 | 3.00 | 2.00-5.00 |
| 대조군 | 25 | 6.00 | 4.00-7.50 |

APA 형식으로 작성:

> 치료군의 통증 평가(*Mdn* = 3.00, IQR = 2.00-5.00)가 대조군(*Mdn* = 6.00, IQR = 4.00-7.50)보다 낮았다.

## 2단계: Mann-Whitney U 결과 보고

### 기본 형식

> Mann-Whitney U 검정 결과, 치료군(*Mdn* = 3.00)의 통증 평가가 대조군(*Mdn* = 6.00)보다 유의하게 낮았다, *U* = 156.50, *z* = -3.24, *p* = .001, *r* = .46.

### 각 구성요소 설명

**U 통계량**: 원시 검정통계량입니다. 일부 소프트웨어는 두 가능한 U 값 중 작은 것을 보고하고, 다른 소프트웨어는 큰 것을 보고합니다.

**z-점수**: U의 표준화 값으로, 큰 표본에서 유의성 검정에 사용됩니다. 부호(양수 또는 음수)를 포함하세요.

**p 값**: 정확한 *p* 값을 소수점 셋째 자리까지 보고합니다. .001 미만이면 *p* < .001로 표기합니다.

**효과크기 *r***: *r* = *z* / &radic;*N*으로 계산합니다(*N*은 전체 표본 크기).

## 효과크기 *r* 이해하기

Mann-Whitney U의 효과크기 *r*은 상관계수와 동일한 해석 기준을 따릅니다:

| *r* | 해석 |
|-----|------|
| .10 | 작은 효과 |
| .30 | 중간 효과 |
| .50 | 큰 효과 |

예시에서 *r* = .46은 중간~큰 효과에 해당하며, 두 집단 간 통증 평가에 상당한 차이가 있음을 나타냅니다.

## 완전한 보고 예시

> **결과**
>
> 치료군(*n* = 25)과 대조군(*n* = 25)의 통증 평가를 Mann-Whitney U 검정으로 비교하였다. 치료군의 통증 평가(*Mdn* = 3.00, IQR = 2.00-5.00)가 대조군(*Mdn* = 6.00, IQR = 4.00-7.50)보다 유의하게 낮았다, *U* = 156.50, *z* = -3.24, *p* = .001, *r* = .46. 효과크기는 중간~큰 수준의 집단 간 차이를 나타냈다.

## 흔한 실수 5가지

### 1. 중앙값 대신 평균 보고

Mann-Whitney U 검정은 순위 기반이므로 평균이 아닌 중앙값과 IQR을 보고해야 합니다.

### 2. 효과크기 누락

*U*와 *p*만 보고하는 연구자가 많지만, APA 7판은 효과크기를 요구합니다. 반드시 *r*을 포함하세요.

### 3. 정확 p와 점근 p 미구분

작은 표본(집단당 *n* < 20)에서는 점근 근사보다 정확 *p* 값이 더 정확합니다.

### 4. z-점수 누락

*U* 통계량만으로는 해석이 어렵습니다. 항상 *z*-점수를 함께 보고하세요.

### 5. 방향 미기술

어느 집단이 더 높은/낮은 순위를 가졌는지 명확히 기술해야 합니다.

## 직접 분석해보기

무료 [Mann-Whitney U 계산기](/ko/calculators/mann-whitney)를 사용하면 U 통계량, z-점수, 정확한 p-값, 효과크기를 자동으로 계산하고 APA 형식 결과 문장을 제공합니다.
